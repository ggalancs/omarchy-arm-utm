# Downloaded base images (~900 MB) -- the script fetches and verifies them
dl/

# Build disks (tens of GB)
vm/
build-test/

# The distributable image: 6.5 GB, too large for a repository.
# It is published as a release. The README and the sha256 are versioned.
dist/*.zip
dist/*.utm/

# Run output
logs/
provision/*.iso
provision/repair-iso/*.log

# Only the two screenshots the documentation uses are versioned; the rest are
# debugging snapshots (39 MB) that give a person cloning the repo nothing.
shots/*
!shots/hires.png
!shots/dist-final.png

# macOS
.DS_Store

# Drafts of the messages for the Omarchy discussions
publicar/

# Build directory for v2
build-v2/
build-v3/
build-v4/
build-v5/
__pycache__/
*.pyc
